---
output:
  md_document:
    variant: markdown_github
---

```{r, echo = FALSE}
raw_github <- function(user, project) {
  function(filename) {
    paste(sep = "/",
      "https://github.com",
      user,
      project,
      "raw/master",
      filename
    )
  }
}
knitr::opts_knit$set(upload.fun = raw_github(user="maRce10", project="hummingbird_song_evolution"))
knitr::opts_chunk$set(fig.path = "figures/", collapse = TRUE, comment = "#>")
```

# Histogram #
```{r, fig.cap='Histogram'}
hist(rnorm(100))
```

# Hummingbird song evolution

Unvailing the evolutionary dynamics that generated complex vocalizations in hummingbirds


